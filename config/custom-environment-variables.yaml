server:
  port: PORT
daisho:
  hostname: DAISHO_HOSTNAME
  port: DAISHO_PORT
database:
  nonDrupal:
    databaseName: DATABASE_NAME
    host: DATABASE_HOST
    password: DATABASE_PASSWORD
    user: DATABASE_USER
