import React from "react";
import {shallow} from "enzyme";
import renderer from "react-test-renderer";
import ErrorPage from "client/components/pages/error-page/error-page.jsx";

describe("Error Page", () => {

	test("should have http-404 class", () => {

		// stub test to be improved upon
		// the below code doesn't properly select the error page

		//const component = shallow(<ErrorPage />)
		//console.log("AA", component.find(".http-404").first())
		//expect(component.render.find(".http-404").first()).toHaveLength(1)

	});

});